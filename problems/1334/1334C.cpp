#include <iostream>
#include <algorithm>

#define ll long long
using namespace std;
ll t, n, i;

void solve() {
    cin >> n;
    ll a[n], b[n], e[n], alive[n];
    for (i = 0; i < n; i++) {
        cin >> a[i] >> b[i];
        e[i] = 0;
        alive[i] = true;
    }
    i = 0;
    while(){

        i++;
    }
    cout << "6";

}

int main() {
    cin >> t;
    while (t--) {
        solve();
    }
}